Source for the assets: https://cardconjurer.app/

Dependencies
1. Install Python 3.11+
2. Run: pip install -r requirements.txt
3. Install Inkscape
4. Run: download_mana_symbols.py (after modifying INKSCAPE_CMD to point to your inkscape.exe)
